<Window x:Class="Knihovna.Views.UzivateleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Správa uživatelů" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundLight}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryDark}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="DATABÁZE ČTENÁŘŮ" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔍 Hledat příjmení:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding FiltrPrijmeni, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                    </StackPanel>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding UzivateleView}" SelectedItem="{Binding SelectedUzivatel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Jméno" Binding="{Binding Jmeno}" Width="*"/>
                        <DataGridTextColumn Header="Příjmení" Binding="{Binding Prijmeni}" Width="*"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                        <DataGridTextColumn Header="Telefon" Binding="{Binding Telefon}" Width="100"/>
                        <DataGridTextColumn Header="Dluh" Binding="{Binding Dluh}" Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
                    <Button Content="➕ Nový čtenář" Command="{Binding PridatCommand}" Width="140" Background="#4CAF50"/>
                    <Button Content="🗑️ Smazat" Command="{Binding SmazatCommand}" Width="120" Background="#E53935"/>
                </StackPanel>
            </Grid>

            <Border Grid.Column="2" Background="White" CornerRadius="5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Detail uživatele" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryDark}" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                    <TextBlock Text="Jméno:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedUzivatel.Jmeno, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Příjmení:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedUzivatel.Prijmeni, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Email:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedUzivatel.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Telefon:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedUzivatel.Telefon, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Adresa:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedUzivatel.Adresa, UpdateSourceTrigger=PropertyChanged}" Height="50" TextWrapping="Wrap" Margin="0,5,0,10"/>

                    <TextBlock Text="Dluh (Kč):" FontWeight="SemiBold" Foreground="#D32F2F"/>
                    <TextBox Text="{Binding SelectedUzivatel.Dluh, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20" FontWeight="Bold"/>

                    <Button Content="💾 Uložit změny" Command="{Binding UlozitCommand}" Height="40" FontSize="16"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
