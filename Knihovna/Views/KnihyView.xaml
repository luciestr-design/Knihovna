<Window x:Class="Knihovna.Views.KnihyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Správa knih" Height="650" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundLight}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryDark}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                <TextBlock Text="&#xE8F1;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="SPRÁVA KNIH" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10" BorderBrush="#E0E0E0" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="🔍 Hledat název nebo autora:" FontWeight="SemiBold" Margin="0,0,0,5" FontSize="12" Foreground="Gray"/>
                            <TextBox Text="{Binding HledanyText, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="📅 Filtr roku:" FontWeight="SemiBold" Margin="0,0,0,5" FontSize="12" Foreground="Gray"/>
                            <StackPanel Orientation="Horizontal">
                                <ComboBox ItemsSource="{Binding SeznamRoku}" SelectedItem="{Binding VybranyRok, UpdateSourceTrigger=PropertyChanged}" 
                                          Width="100" Padding="5" VerticalContentAlignment="Center"/>
                                <Button Command="{Binding ZrusitFiltrRokuCommand}" Content="❌" Width="30" Margin="5,0,0,0" Background="#EEEEEE" Foreground="Black"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding KnihyView}" 
                          SelectedItem="{Binding VybranaKniha, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Název" Binding="{Binding Nazev}" Width="*"/>
                        <DataGridTextColumn Header="Autor" Binding="{Binding Autor}" Width="150"/>
                        <DataGridTextColumn Header="Rok" Binding="{Binding RokVydani}" Width="70"/>
                        <DataGridTextColumn Header="Žánr" Binding="{Binding Zanr}" Width="100"/>
                        <DataGridTextColumn Header="K dispozici" Binding="{Binding KusuKDispozici}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
                    <Button Content="➕ Nová kniha" Command="{Binding PridatCommand}" Width="140" Background="#4CAF50"/>
                    <Button Content="🗑️ Smazat" Command="{Binding SmazatCommand}" Width="120" Background="#E53935"/>
                </StackPanel>
            </Grid>

            <Border Grid.Column="2" Background="White" CornerRadius="5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1">
                <StackPanel IsEnabled="{Binding JeKnihaVybrana}">
                    <TextBlock Text="Detail knihy" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryDark}" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                    <TextBlock Text="Název knihy:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding VybranaKniha.Nazev, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Autor:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding VybranaKniha.Autor, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Žánr:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding VybranaKniha.Zanr, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="ISBN:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding VybranaKniha.ISBN, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Rok:" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding VybranaKniha.RokVydani, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Celkem:" FontWeight="SemiBold"/>
                            <TextBox Text="{Binding VybranaKniha.CelkemKusu, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Kusů k dispozici:" FontWeight="SemiBold" Margin="0,10,0,0"/>
                    <TextBox Text="{Binding VybranaKniha.KusuKDispozici, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20"/>

                    <Button Content="💾 Uložit změny" Command="{Binding UlozitCommand}" Height="40" FontSize="16"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>