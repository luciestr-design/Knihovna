<Window x:Class="Knihovna.Views.VypujckyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Správa výpůjček" Height="650" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundLight}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryDark}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                <TextBlock Text="&#xE787;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="EVIDENCE VÝPŮJČEK" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔍 Hledat příjmení čtenáře:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding FiltrPrijmeni, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                    </StackPanel>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding VypujckyView}" SelectedItem="{Binding SelectedVypujcka, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Příjmení" Binding="{Binding Uzivatel.Prijmeni}" Width="120"/>
                        <DataGridTextColumn Header="Jméno" Binding="{Binding Uzivatel.Jmeno}" Width="100"/>
                        <DataGridTextColumn Header="Kniha" Binding="{Binding Kniha.Nazev}" Width="*"/>
                        <DataGridTextColumn Header="Půjčeno" Binding="{Binding DatumPujceni, StringFormat=d}" Width="80"/>
                        <DataGridTextColumn Header="Stav" Binding="{Binding Stav}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
                    <Button Content="➕ Nová výpůjčka" Command="{Binding PridatCommand}" Width="140" Background="#4CAF50"/>
                    <Button Content="↩️ Vrátit knihu" Command="{Binding VratitKnihuCommand}" Width="130" Background="#FF9800"/>
                    <Button Content="🗑️ Smazat" Command="{Binding SmazatCommand}" Width="120" Background="#E53935"/>
                </StackPanel>
            </Grid>

            <Border Grid.Column="2" Background="White" CornerRadius="5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Detail transakce" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryDark}" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                    <TextBlock Text="Čtenář:" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding SeznamUzivatelu}" SelectedItem="{Binding SelectedVypujcka.Uzivatel, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="Prijmeni" Margin="0,5,0,10" Padding="5"/>

                    <TextBlock Text="Kniha:" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding SeznamKnih}" SelectedItem="{Binding SelectedVypujcka.Kniha, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="Nazev" Margin="0,5,0,10" Padding="5"/>

                    <TextBlock Text="Datum půjčení:" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding SelectedVypujcka.DatumPujceni, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Datum splatnosti:" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding SelectedVypujcka.DatumSplatnosti, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Skutečné vrácení:" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding SelectedVypujcka.DatumVraceni, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <TextBlock Text="Stav:" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedVypujcka.Stav, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,20"/>

                    <Button Content="💾 Uložit změny" Command="{Binding UlozitCommand}" Height="40" FontSize="16"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
